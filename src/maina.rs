mod tour;

pub fn main() {
    tracing_subscriber::fmt::init();

    let mut app = Tour
    
    // iced::run(Tour::update, Tour::view)
    // iced::application(Tour::title, Tour::update, Tour::view)
    //     .centered()
    //     .run()
}